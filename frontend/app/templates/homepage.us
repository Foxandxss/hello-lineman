<!DOCTYPE html>
<html ng-app="app">
  <head>
    <title><%= pkg.name %></title>
    <link rel="stylesheet" type="text/css" href="<%= css %>" media="all" />
  </head>
  <body>
    <div class="row">
      <div class="large-12">
        <h1>An example of an AngularJS app deployed into a Rails app</h1>
        <div id="view" ng-view></div>
      </div>
    </div>

    <script type="text/javascript" src="<%= js %>"></script>
  </body>
</html>
